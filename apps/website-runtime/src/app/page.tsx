import { WebsiteNotFound } from "@/components/website/website-not-found";

export default function HomePage() {
  return <WebsiteNotFound message="This runtime is only available via website routes." />;
}
